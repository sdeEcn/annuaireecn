<nav class="navbar navbar-expand-lg navbar-light blue-background">
    <a class="navbar-brand" href="/">{{ 'common.directory'|trans }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav w-100">
            <li class="nav-item">
                <a class="nav-link" href="/rechercheEleve">{{ 'common.students'|trans }}<span class="sr-only"></span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/rechercheClub">{{ 'common.clubs'|trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#Options">{{ 'common.options'|trans }}</a>
            </li>
            <li class="nav-item ml-auto ">
                {% if app.user %}
                    <div class="dropdown show">

                            <a class="dropdown-toggle nav-link" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color: white;">
                               {{ app.user.prenom }} {{ app.user.nom }}
                            </a>


                            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink"
                                 style="position: absolute; transform: translate3d(-40px, 36px, 0px); top: 0px; left: 0px; will-change: transform;">
                                <a class="dropdown-item " href="{{ path("app.user.own") }}" style="color: #2e3436 ;"><i class="fas fa-user-alt"></i>  {{ 'common.profil'|trans }}</a>
                                <a class="dropdown-item" href="{{ path("app.user.params") }}" style="color: #2e3436 ;"><i class="fas fa-cog"></i>  {{ 'common.params'|trans }}</a>
                                <a class="dropdown-item" href="/logout" style="color: #2e3436 ;"><i class="fas fa-sign-out-alt"></i>  {{ 'common.logout'|trans }}</a>
                            </div>

                    </div>

                {% else %}
                <a class="nav-link" href="/login">{{ 'common.login'|trans }}</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>
